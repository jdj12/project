<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.project.mapper.codybookMapper">
  
  	<insert id="input">
  		insert into codybook(bookname,id,pcode)
		value(#{bookname},#{id},#{pcode})
  	</insert>
  	
  	<delete id="output">
  		delete from codybook where bookname=#{bookname}
  	</delete>
 
 	<select id="all">
 		select * from codybook
 	</select>
 
  </mapper>